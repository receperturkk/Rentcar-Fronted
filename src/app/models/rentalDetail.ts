import { DatePipe } from "@angular/common";

export interface RentalDetail {
  id: number;
  carName: string;
  brandName: string;
  firstName: string;
  lastName: string;
  rentDate: string;
  returnDate: string;
}
